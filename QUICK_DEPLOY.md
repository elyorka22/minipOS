# Быстрый деплой

## Backend → Railway

1. Зайдите на https://railway.app
2. Создайте новый проект
3. Добавьте PostgreSQL базу данных
4. Добавьте сервис из GitHub (укажите папку `backend/`)
5. Railway автоматически установит `DATABASE_URL`
6. Скопируйте URL вашего сервиса (например: `https://your-app.up.railway.app`)

## Frontend → Vercel

1. Зайдите на https://vercel.com
2. Создайте новый проект из GitHub (укажите папку `frontend/`)
3. В настройках проекта → Environment Variables добавьте:
   ```
   VITE_API_URL=https://your-app.up.railway.app
   ```
4. **Важно**: Откройте `frontend/app.js` и замените в функции `getApiUrl()`:
   ```javascript
   // Замените на ваш реальный Railway URL
   return 'https://your-app.up.railway.app';
   ```
5. Закоммитьте изменения и запушьте
6. Vercel автоматически задеплоит

## Альтернатива: Использовать скрипт замены

```bash
cd frontend
node replace-env.js https://your-app.up.railway.app
git add .
git commit -m "Update API URL"
git push
```

## Проверка

1. Откройте URL вашего Vercel приложения
2. Откройте DevTools → Network
3. Попробуйте добавить товар
4. Убедитесь, что запросы идут на Railway backend

Готово! 🎉

