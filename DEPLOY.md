# Инструкции по деплою

Проект разделен на две части:
- **Backend** → Railway (Node.js + PostgreSQL)
- **Frontend** → Vercel (статический сайт)

## Деплой Backend на Railway

### 1. Подготовка

1. Установите Railway CLI (опционально):
   ```bash
   npm i -g @railway/cli
   ```

2. Или используйте веб-интерфейс Railway: https://railway.app

### 2. Создание проекта на Railway

1. Зайдите на https://railway.app
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo" или "Empty Project"

### 3. Добавление PostgreSQL базы данных

1. В проекте Railway нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway автоматически создаст базу и установит переменную окружения `DATABASE_URL`

### 4. Деплой Backend

**Вариант A: Через GitHub**

1. Создайте репозиторий на GitHub
2. Загрузите папку `backend/` в репозиторий
3. В Railway:
   - Нажмите "+ New" → "GitHub Repo"
   - Выберите ваш репозиторий
   - Укажите Root Directory: `backend`
   - Railway автоматически определит Node.js проект

**Вариант B: Через Railway CLI**

```bash
cd backend
railway login
railway init
railway link  # если проект уже создан
railway up
```

### 5. Настройка переменных окружения

В Railway Dashboard → Variables добавьте:

```
DATABASE_URL=<автоматически установлена Railway>
NODE_ENV=production
PORT=3000
```

### 6. Получение URL Backend

После деплоя Railway предоставит URL вида:
```
https://your-app-name.up.railway.app
```

Сохраните этот URL - он понадобится для настройки Frontend.

---

## Деплой Frontend на Vercel

### 1. Подготовка

1. Установите Vercel CLI (опционально):
   ```bash
   npm i -g vercel
   ```

2. Или используйте веб-интерфейс Vercel: https://vercel.com

### 2. Создание проекта на Vercel

**Вариант A: Через GitHub**

1. Создайте отдельный репозиторий для frontend (или используйте монорепо)
2. Загрузите папку `frontend/` в репозиторий
3. Зайдите на https://vercel.com
4. Нажмите "Add New..." → "Project"
5. Импортируйте ваш репозиторий
6. Укажите:
   - **Framework Preset**: Other
   - **Root Directory**: `frontend` (если монорепо) или оставьте пустым
   - **Build Command**: (оставьте пустым, статический сайт)
   - **Output Directory**: `.` (текущая директория)

**Вариант B: Через Vercel CLI**

```bash
cd frontend
vercel login
vercel
```

### 3. Настройка переменных окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
VITE_API_URL=https://your-app-name.up.railway.app
```

**Важно**: Замените `your-app-name.up.railway.app` на реальный URL вашего Railway backend.

### 4. Обновление app.js для работы с переменными

Vercel не поддерживает переменные окружения напрямую в статических файлах. Нужно использовать один из вариантов:

**Вариант 1: Использовать скрипт в index.html (уже настроено)**

В `index.html` уже есть:
```html
<script>
  window.API_URL = '%VITE_API_URL%' || 'http://localhost:3000';
</script>
```

Но нужно заменить `%VITE_API_URL%` на реальный URL при деплое.

**Вариант 2: Использовать Vercel Environment Variables через API Routes**

Создайте файл `api/config.js`:
```javascript
export default function handler(req, res) {
  res.json({ apiUrl: process.env.VITE_API_URL });
}
```

И обновите `app.js`:
```javascript
fetch('/api/config')
  .then(res => res.json())
  .then(config => {
    window.API_URL = config.apiUrl;
  });
```

**Вариант 3: Простое решение - хардкод URL в app.js для продакшена**

В `frontend/app.js` замените:
```javascript
const API_URL = getApiUrl() + '/api';
```

На:
```javascript
// Для продакшена используйте URL вашего Railway backend
const API_URL = (process.env.NODE_ENV === 'production' 
  ? 'https://your-app-name.up.railway.app' 
  : 'http://localhost:3000') + '/api';
```

### 5. Деплой

После настройки переменных окружения:

```bash
vercel --prod
```

Или через веб-интерфейс Vercel нажмите "Deploy"

### 6. Получение URL Frontend

После деплоя Vercel предоставит URL вида:
```
https://your-frontend-name.vercel.app
```

---

## Проверка работы

1. Откройте URL вашего Frontend (Vercel)
2. Откройте DevTools → Network
3. Попробуйте добавить товар
4. Проверьте, что запросы идут на правильный Backend URL (Railway)

---

## Обновление CORS на Backend

Убедитесь, что в `backend/server.js` CORS настроен правильно:

```javascript
app.use(cors({
  origin: [
    'https://your-frontend-name.vercel.app',
    'http://localhost:3000' // для локальной разработки
  ],
  credentials: true
}));
```

Или для всех доменов (менее безопасно, но проще):
```javascript
app.use(cors());
```

---

## Troubleshooting

### Backend не отвечает
- Проверьте переменную `DATABASE_URL` в Railway
- Проверьте логи в Railway Dashboard
- Убедитесь, что порт установлен через переменную `PORT`

### Frontend не может подключиться к Backend
- Проверьте переменную `VITE_API_URL` в Vercel
- Проверьте CORS настройки на Backend
- Откройте DevTools → Console и проверьте ошибки

### CORS ошибки
- Добавьте URL Frontend в CORS настройки Backend
- Убедитесь, что Backend отвечает на запросы (проверьте `/health` endpoint)

---

## Структура проекта после деплоя

```
elpos/
├── backend/          → Railway
│   ├── server.js
│   ├── package.json
│   └── railway.json
└── frontend/         → Vercel
    ├── index.html
    ├── app.js
    ├── manifest.json
    ├── sw.js
    └── vercel.json
```

---

## Быстрый старт (кратко)

### Backend (Railway):
1. Создать проект на Railway
2. Добавить PostgreSQL
3. Загрузить код из `backend/`
4. Установить переменные окружения
5. Получить URL

### Frontend (Vercel):
1. Создать проект на Vercel
2. Загрузить код из `frontend/`
3. Установить `VITE_API_URL` = URL Railway backend
4. Обновить `app.js` с реальным URL (или использовать env переменные)
5. Деплой

Готово! 🚀

