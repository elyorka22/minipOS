# Учет товаров - Простое приложение для продуктового магазина

Простое PWA приложение для учета товаров с поддержкой сканирования штрих-кодов.

## 🚀 Быстрый старт (локальная разработка)

### Вариант 1: Монолитная версия (для быстрого тестирования)

```bash
npm install
npm start
```

Откройте `http://localhost:3000` в браузере.

### Вариант 2: Раздельная версия (Backend + Frontend)

**Backend:**
```bash
cd backend
npm install
# Создайте .env файл с DATABASE_URL
npm start
```

**Frontend:**
```bash
cd frontend
# Используйте любой статический сервер
python -m http.server 8080
# или
npx http-server -p 8080
```

## 📦 Структура проекта

```
elpos/
├── backend/              # Backend API (Railway)
│   ├── server.js         # Express сервер с PostgreSQL
│   ├── package.json
│   └── railway.json      # Конфигурация Railway
├── frontend/             # Frontend PWA (Vercel)
│   ├── index.html
│   ├── app.js
│   ├── manifest.json
│   ├── sw.js
│   └── vercel.json       # Конфигурация Vercel
├── public/               # Старая монолитная версия (для локальной разработки)
├── server.js             # Старый сервер (SQLite)
├── DEPLOY.md             # Инструкции по деплою
└── README.md
```

## 🌐 Деплой в продакшн

Проект разделен на две части для деплоя:
- **Backend** → Railway (Node.js + PostgreSQL)
- **Frontend** → Vercel (статический сайт)

📖 **Подробные инструкции по деплою**: см. [DEPLOY.md](./DEPLOY.md)

### Краткая инструкция:

1. **Backend на Railway:**
   - Создайте проект на Railway
   - Добавьте PostgreSQL базу данных
   - Загрузите код из `backend/`
   - Получите URL (например: `https://your-app.up.railway.app`)

2. **Frontend на Vercel:**
   - Создайте проект на Vercel
   - Загрузите код из `frontend/`
   - Установите переменную окружения `VITE_API_URL` = URL Railway backend
   - Обновите `frontend/app.js` с реальным URL backend
   - Деплой

## ✨ Возможности

- 📱 **Сканирование штрих-кодов** через камеру телефона
- 💰 **Продажа товара** - автоматическое списание при сканировании
- 📦 **Прием товара** - увеличение остатков на складе
- ➕ **Добавление товаров** - создание новых позиций
- 📋 **Список товаров** - просмотр всех товаров и остатков
- 🔄 **PWA** - работает как приложение на Android

## 🛠 Технологии

### Backend
- Node.js + Express
- PostgreSQL (продакшн) / SQLite (локальная разработка)
- Railway для деплоя

### Frontend
- HTML + CSS + JavaScript (Vanilla JS)
- QuaggaJS для сканирования штрих-кодов
- PWA (Service Worker + Manifest)
- Vercel для деплоя

## 📖 Использование

### Продажа товара
1. Нажмите кнопку **"ПРОДАЖА (СКАНИРОВАТЬ)"**
2. Наведите камеру на штрих-код товара
3. Товар автоматически списывается со склада (количество уменьшается на 1)

### Принять товар
1. Нажмите кнопку **"ПРИНЯТЬ ТОВАР"**
2. Отсканируйте штрих-код товара
3. Введите количество
4. Нажмите **"ПОДТВЕРДИТЬ"**

### Добавить товар
1. Нажмите кнопку **"ДОБАВИТЬ ТОВАР"**
2. Введите название товара
3. Введите или отсканируйте штрих-код
4. Укажите начальное количество
5. Нажмите **"ДОБАВИТЬ НА СКЛАД"**

### Список товаров
1. Нажмите кнопку **"СПИСОК ТОВАРОВ"**
2. Просмотрите все товары и их остатки

## 🔌 API Endpoints

- `GET /api/products` - Получить все товары
- `GET /api/products/barcode/:barcode` - Получить товар по штрих-коду
- `POST /api/products/sell/:barcode` - Продать товар (уменьшить на 1)
- `POST /api/products` - Добавить новый товар
- `POST /api/products/receive/:barcode` - Принять товар (увеличить количество)
- `GET /health` - Health check (только для backend)

## 📝 Примечания

- Для работы сканера требуется разрешение на использование камеры
- Приложение оптимизировано для мобильных устройств
- Иконки PWA (icon-192.png, icon-512.png) можно создать позже или использовать онлайн-генераторы
- В продакшене используется PostgreSQL, локально можно использовать SQLite

## 🔧 Разработка

### Локальная разработка Backend

```bash
cd backend
npm install
# Создайте .env с DATABASE_URL
npm run dev
```

### Локальная разработка Frontend

```bash
cd frontend
# Используйте любой статический сервер
npx http-server -p 8080
```

## 📚 Дополнительная документация

- [Инструкции по деплою](./DEPLOY.md)
- [Backend README](./backend/README.md)
- [Frontend README](./frontend/README.md)
